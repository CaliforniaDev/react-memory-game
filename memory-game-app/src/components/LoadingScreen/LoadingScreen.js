import { useEffect } from "react";
import styled from "styled-components";

const LoadingScreen = () => {

  return (
    <LoadingContainer>

    </LoadingContainer>
  )

  const LoadingContainer = styled.div`
  
  `
}