import styled from 'styled-components';
const Card = () => {
  return (
    <CardContainer>
    </CardContainer>
  );
}

export default Card;

const CardContainer = styled.div`
  flex: 0 0 32rem;
  height: 40rem;
  border-radius: 1.6rem;
  color: #fff;
  background: #fff;
`
