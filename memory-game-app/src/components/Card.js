import styled from 'styled-components';
const Card = () => {
  return (
    <MainContainer>
      Card
    </MainContainer>
  );
}

export default Card;

const MainContainer = styled.div`
  color: #fff;
`
